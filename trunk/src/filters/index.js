import {normalTime} from './time-format';
import {cdnImg} from './img-src-format';
import {zero} from './zero-format';
import {date} from './date-format';

export default {
    normalTime, cdnImg, zero, date
};
